@model IEnumerable<Fix_Sys_V2.Models.Building>

@{
    ViewBag.Title = "อาคาร";
}

<h2>อาคาร</h2>

<p>
    @Html.ActionLink("เพิ่มอาคาร", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Build)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FloorNum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Build)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FloorNum)
        </td>
        <td>
            @Html.ActionLink("แก้ไข", "Edit", new { id=item.ID }) |
            @Html.ActionLink("รายละเอียด", "Details", new { id=item.ID }) |
            @Html.ActionLink("ลบ", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
